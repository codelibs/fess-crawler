CREATE TABLE  "ACCESS_RESULT_DATA" 
   (    "ID" NUMBER(19,0) NOT NULL ENABLE, 
    "TRANSFORMER_NAME" VARCHAR2(255) NOT NULL ENABLE, 
    "DATA" BLOB, 
    "ENCODING" VARCHAR2(20), 
     CONSTRAINT "ACCESS_RESULT_DATA_PK" PRIMARY KEY ("ID") ENABLE
   ) ;

ALTER TABLE  "ACCESS_RESULT_DATA" ADD CONSTRAINT "ACCESS_RESULT_DATA_FK" FOREIGN KEY ("ID")
      REFERENCES  "ACCESS_RESULT" ("ID") ENABLE;
